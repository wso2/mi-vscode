{
  "name": "xml-language-server",
  "version": "1.1.0",
  "description": "LSP wrapper for xml-language-service",
  "main": "dist/server.js",
  "scripts": {
    "build": "esbuild src/server.ts --bundle --platform=node --format=cjs --outfile=dist/server.js \"--banner:js=const _fileUrl=require('url').pathToFileURL(__filename).href;\" --define:import.meta.url=_fileUrl && node -e \"const fs=require('fs'); fs.rmSync('dist/resources',{recursive:true,force:true}); fs.cpSync('resources','dist/resources',{recursive:true})\"",
    "bundle": "npm run build && node -e \"require('fs').copyFileSync('xerces-wasm/wasm/xerces_validator.wasm', 'dist/xerces_validator.wasm')\"",
    "build:watch": "tsc --watch",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@xml-tools/parser": "^1.0.11",
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.12"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
